#!/usr/bin/env ruby

$LOAD_PATH << File.expand_path(File.dirname(__FILE__) + "/../lib")
require 'tango'

def dance(name, &block)
  Tango::DanceCard.instance.dance(name, &block)
end

load ARGV[0]

Tango::DanceCard.instance.run ARGV[1]
